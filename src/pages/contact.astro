---
import BaseLayout from "../layouts/BaseLayout.astro";
import BackgroundGlow from "../components/BackgroundGlow.astro";
import ContactForm from "../components/ContactForm.astro";
---

<BaseLayout title="Contact | moestdio">
    <main class="min-h-screen pt-16 md:pt-32 snap-loading" data-no-snap-mobile>
        <section
            class="snap-target reveal-container min-h-dvh w-full flex flex-col justify-center px-4 pt-20 pb-6 md:px-8 md:py-24 relative overflow-hidden"
        >
            <BackgroundGlow position="absolute" hasSecondary={false} />

            <div class="max-w-3xl mx-auto w-full relative z-10">
                <div class="text-center mb-10 md:mb-16">
                    <h1
                        class="reveal text-[clamp(2.5rem,6vw,5rem)] font-bold mb-4 bg-gradient-to-r from-slate-800 to-slate-400 dark:from-white dark:to-slate-400 bg-clip-text text-transparent box-decoration-clone"
                    >
                        Get in Touch
                    </h1>
                    <p
                        class="reveal text-lg md:text-xl text-slate-600 dark:text-slate-400"
                    >
                        Select a category below to get in touch.
                    </p>
                </div>

                <ContactForm />
            </div>
        </section>
    </main>
</BaseLayout>
